"""
AUTOGENERATED CODE - DO NOT EDIT

If you want to customize this structure:
  1. Copy this file to your own module
  2. Remove the 'structure' or 'refines' declaration from the concept in the MTHDS file
     and declare it in inline mode (see https://docs.pipelex.com/home/6-build-reliable-ai-workflows/concepts/define_your_concepts/#basic-concept-definition)
  3. Make sure your custom class is importable and registered

To regenerate: pipelex build structures <target_directory>
"""

from enum import Enum
from typing import Any, Dict, List, Literal, Optional

from pipelex.core.stuffs.structured_content import StructuredContent
from pydantic import Field


class CandidateProfile(StructuredContent):
    """A structured summary of a job candidate's professional background extracted from their CV."""

    skills: str = Field(..., description="Technical and soft skills possessed by the candidate")
    experience: str = Field(..., description="Work history and professional experience")
    education: str = Field(..., description="Educational background and qualifications")
    achievements: Optional[str] = Field(default=None, description="Notable accomplishments and certifications")
